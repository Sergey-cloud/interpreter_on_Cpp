#pragma once
#include <iostream>
#include <sstream>
#include <string>

using namespace std;


int Rule_COUT(string);

int Rule_ASSIGN(string);

int Rule_IF(string); 

int Rule_STROKA(string); 

int Rule_WHILE(string);

int Rule_BREAK(string);

int Rule_END_BODY(string);